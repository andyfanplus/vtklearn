SET(CMAKE_INCLUDE_CURRENT_DIR ON) 

SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR})

ADD_EXECUTABLE(StructuredVolumeRendering main.cpp)

TARGET_LINK_LIBRARIES(StructuredVolumeRendering
${VTK_LIBRARIES}
)


